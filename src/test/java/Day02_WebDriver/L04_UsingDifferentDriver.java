package Day02_WebDriver;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class L04_UsingDifferentDriver {


    // If the company we are working for
    // requests us to use a specific WebDriver instead of Selenium WebDriver,
    // we must add that WebDriver to our project.

    // WebDriver driver = new ChromeDriver();
/*
        WebDriverManager.chromedriver().

    void setup();
    WebDriver driver = new ChromeDriver();
        driver.get("https://www.testotomasyonu.com");

        Thread.sleep(3000);
        driver.quit();

 */
}
